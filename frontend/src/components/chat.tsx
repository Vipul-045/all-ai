import inputvalue from "./InputArea"
import outputvalue from "./Home"

function Chat() {
    const user = inputvalue;
    const ai = outputvalue;
  return (
    <div>chat</div>
  )
}

export default Chat;